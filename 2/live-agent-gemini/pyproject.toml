[project]
name = "safenav-live-gemini"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "google-genai>=1.0",
    "pyaudio>=0.2",
    "websockets>=13.0",
    "python-dotenv>=1.0",
]

[tool.uv]
dev-dependencies = []
