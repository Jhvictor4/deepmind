[project]
name = "safenav-agent"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "livekit-agents[google,images,silero]>=1.4",
    "livekit-plugins-turn-detector>=1.4",
    "python-dotenv>=1.0",
]

[tool.uv]
dev-dependencies = []
